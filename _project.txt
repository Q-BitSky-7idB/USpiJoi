¶sorj-project-file

0) вхід: джерело - файл, місце призначення - файл та місце(за замовчанням - R:\), команди: розмір шматка або кількість шматків; 
1) потрібен розпізнавач даних і машина стану
2) потрібен інтерактивний дільник для визначення і виводу результату обчислень до операції

0. арг1: файл, арг2: місце | арг3: число
1. детермінація надходжень:
1.1. перше - файл для сплітування
1.2. друге - місце для перезапису - якщо не вказаний, тоді пропуск(використовуй дефолт)
1.3. число байт одного чанку
2. детермінація поведінки:
2.1. якщо файл, і існує, тоді використовуємо спліт-функцію
2.2. якщо місце, і існує, і порожнє - очікує появу файлу з типом ".CPARTA"
2.3. якщо місце, і там є файл з типом ".CPARTA"
3. виконання:
3.1. 

https://www.youtube.com/@YablocoderHighlights

↓ file:[ ▒ █ █ █ █ █ █ ]
 ↓ chunk: █ → [■ ■■■■■]
  ↓ buffer: [▓] → {■} → (█)

♦ telegram_mode:
 filename.ext
 ↓                   ↑
  + filename.ext.part	hash-sum→clipboard
  + filename.ext.part	
  + filename.ext.part	
  + filename.ext.part	
  + filename.ext.part	
♦ simple_mode:
→ filename.ext → filename.ext.sj_part
  ↓
  state
  
  ∠
  ½╜